# ============================================================
# VoxelSage 前端 — 配置说明
# ============================================================
# 复制为 .env.local 后按部署地址修改。Vite 会在构建时注入这些值。
# 留空时，前端使用当前页面的主机名以及 Port A/Port B 的默认端口。
VITE_PORT_A_HTTP_URL=http://127.0.0.1:8900
VITE_PORT_A_WS_URL=ws://127.0.0.1:8900
VITE_PORT_B_HTTP_URL=http://127.0.0.1:8765
#
# HTTPS 部署应使用 https:// 和 wss:// 地址，或通过反向代理暴露服务。
# 本文件仅包含公开配置示例，不应写入真实密钥。
